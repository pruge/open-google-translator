const config = {
    entries: [
        {
            filePath: "./src/index.ts",
            outFile: "./dist/open-google-translator.d.ts",
            noCheck: false,
        },
    ],
};

module.exports = config;
